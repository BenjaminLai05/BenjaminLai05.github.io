[phases.setup]
nixPkgs = ["ffmpeg", "glib", "libglvnd"]
aptPkgs = ["libgl1-mesa-glx", "libglib2.0-0"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "pip install opencv-python-headless==4.10.0.84 --force-reinstall --no-deps"
]
